# Cloudflare API Token (required)
# Get it from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone.DNS (Edit)
CF_API_TOKEN=your_cloudflare_api_token_here

# Cloudflare Zone ID (required)
# Found in the domain's overview page on Cloudflare dashboard
CF_ZONE_ID=your_zone_id_here

# DNS record name to update (required)
# Example: ddns.example.com or home.example.com
CF_RECORD_NAME=ddns.example.com

# Check interval in seconds (optional, default: 300)
# 60 = 1 minute, 300 = 5 minutes, 600 = 10 minutes
CHECK_INTERVAL=300

# DNS Record Configuration (optional)
# Record type (optional, default: A)
# Options: A (IPv4), AAAA (IPv6)
#DNS_RECORD_TYPE=A

# TTL in seconds (optional, default: 120)
# Minimum: 60, Maximum: 86400 (1 day)
# Use 1 for automatic TTL
#DNS_TTL=120

# Cloudflare proxy status (optional, default: false)
# true = Traffic proxied through Cloudflare (orange cloud)
# false = DNS only (gray cloud)
#DNS_PROXIED=false

# ============================================
# NOTIFICATION SERVICES (All Optional)
# Only configure the services you want to use
# ============================================

# ntfy.sh - Free push notifications (no signup required)
# Create your own unique topic name
# Install ntfy app and subscribe to this topic
#NTFY_TOPIC=my-unique-ddns-topic-12345

# Discord Webhook
# Create webhook in Discord channel settings
#DISCORD_WEBHOOK=https://discord.com/api/webhooks/YOUR_WEBHOOK_URL

# Telegram Bot
# Create bot via @BotFather to get token
# Get your chat ID from @userinfobot
#TELEGRAM_BOT_TOKEN=your_bot_token
#TELEGRAM_CHAT_ID=your_chat_id

# Slack Webhook
# Create incoming webhook in Slack workspace settings
#SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Mailjet Email API
# Get credentials from: https://app.mailjet.com/account/api_keys
#MAILJET_API_KEY=your_mailjet_api_key
#MAILJET_API_SECRET=your_mailjet_api_secret
#MAILJET_FROM_EMAIL=noreply@example.com
#MAILJET_FROM_NAME=DDNS Updater
#MAILJET_TO_EMAIL=your@email.com
